import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.apollon.angular',
  appName: 'apollon angular mobile',
  webDir: 'dist/angular-poc/browser'
};

export default config;
